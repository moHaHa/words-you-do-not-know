import { Table } from 'antd';
import { FC } from 'react';
interface HomeProps {}
const Home: FC<HomeProps> = ({}) => {
  const columns = [{ title: 'Foo' }];
  return <div>foo</div>;
};
export default Home;
